// Pruebas de HatoControl.
//
// La app requiere inicializar Supabase (red) antes de arrancar, por lo que
// aquí dejamos una prueba mínima de marcador de posición. Más adelante
// agregaremos pruebas de las pantallas con datos simulados.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('marcador de posición — el proyecto compila y las pruebas corren', () {
    expect(1 + 1, 2);
  });
}
