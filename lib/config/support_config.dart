/// Public support contact shown on subscription and account screens.
class SupportConfig {
  SupportConfig._();

  static const String email = 'soporte@hatocontrol.com';

  /// Optional phone or WhatsApp line. When null, the UI shows email only.
  static const String? phone = null;
}
